# 受け入れ基準の確認結果

`TermWatch_Requirements_v1.0.md` 第 15 章の受け入れ基準 18 項目について、
実装時点での確認結果を記録する。

- 初回記録: 2026-08-10 ／ 最終更新: 2026-08-11
- 確認環境: Windows 11 Enterprise 10.0.26200 / Node.js v24.14.0 / x64 / iPhone Safari / VS Code Private Port Forwarding
- 実施した自動検証: `npm ci`、`npm run lint`、`npm run typecheck`、`npm test`（143 件）、`npm run build`、`npm run package:win`
- 外部コードレビュー（Codex）を実施し、指摘のうちバグ・セキュリティ該当分を修正済み（`docs/DECISIONS.md` D-015）
- 実施済みの手動確認: MT-01、MT-02、MT-04、MT-05、MT-07、MT-09、MT-10、MT-13、MT-17
- **未実施**: MT-14（Node.js 未導入 PC）、MT-15（障害系）
- **再確認待ち**: PC 側の矢印キー（リモート操作モード中）

判定の意味:

| 記号 | 意味 |
|---|---|
| 合格 | この環境で実際に確認できた |
| 未確認 | 実装済みだが、必要な実機・外部サービスが無いため未検証 |
| 不合格 | 確認して満たせなかった |

**「実装したから合格」とは扱っていない。** 実行して確認したものだけを合格としている。

---

| # | 受け入れ基準 | 判定 | 根拠・備考 |
|---|---|---|---|
| 1 | VS Code ターミナルから `termwatch -- codex` で Codex を起動できる | **合格** | MT-01 を実機で確認（2026-08-11）。`.cmd` シム経由の起動経路も実際に通っている。`codex resume --last` での再開も MT-02 で確認済み |
| 2 | PC 側で通常どおり Codex へ日本語入力できる | **合格** | MT-01 を実機で確認（2026-08-11）。VS Code 統合ターミナル＋IME での日本語入力、ANSI カラー、TUI 表示に問題なし |
| 3 | ポート 43821 を Private 転送し、認証した iPhone からペアリングコードで接続できる | **合格** | MT-04 を実機で確認。dev tunnels 経由の Origin 検証も実環境で通ることを確認した（`docs/DECISIONS.md` D-005 の未確認事項は解消） |
| 4 | iPhone に ANSI カラー付き出力が概ね 500ms 以内で表示される | **合格** | MT-05 を実機で確認 |
| 5 | iPhone 切断後も Codex が動作し続ける | **合格** | 統合テスト `WebSocket切断後も子プロセスは動き続ける` で確認 |
| 6 | iPhone 再接続時に直近 10,000 行が復元される | **合格** | MT-07 を実機で確認（2026-08-11）。統合テスト `再接続時に受信済み以降だけを受け取る`、単体テスト（リングバッファ上限・truncated 通知）でも確認済み。既定 10,000 表示行はチャンク境界での近似保持（D-002） |
| 7 | 閲覧モードでは改変した WebSocket リクエストでも PTY 入力されない | **合格** | 統合テスト `閲覧モードでは直接WebSocketメッセージを送っても入力されない`。サーバー側で操作権を検査 |
| 8 | 操作モードで複数行の日本語指示を一括送信できる | **合格** | MT-09 を実機で確認（2026-08-11）。3 回の不具合を修正した末に「本文を書いたあと TUI の再描画を検知してから確定の Enter を送る」方式で解決（`docs/DECISIONS.md` D-007） |
| 9 | Enter、Esc、Tab、Ctrl+C、方向キー、y、n が機能する | **合格** | MT-10 を実機で確認（2026-08-11）。全 10 キーの動作を確認済み。単体テスト `特殊キーのエンコード` でバイト列も検証 |
| 10 | 操作モードが 10 分後に自動解除される | **合格** | 単体テスト `期限到達で自動解除する`、`残り1分で1度だけ警告する`、`60秒以上の切断で操作権を解除する` |
| 11 | 同時に 2 台が接続しても操作権を持てるのは 1 台だけ | **合格** | 統合テスト `操作権は1台だけが保持できる`、単体テスト `同時に保持できるのは1台だけ` |
| 12 | スマートフォン接続によって PC 側のターミナルサイズが変化しない | **合格** | MT-13 を実機で確認（2026-08-11）。設計上もリサイズ要求はプロトコルに存在せず、スキーマ検証で未知の type は拒否される（単体テスト `未知のtypeを拒否する` で `resize` を明示的に検証）。PTY のリサイズは `LocalIo` の `stdout` の resize イベントからのみ呼ばれる |
| 13 | 子プロセス終了時に終了コードが PC 側とスマートフォン側へ表示される | **合格** | 統合テスト `子プロセス終了時に終了コードを通知する`（WebSocket 側）、`子プロセスの終了コードを伝える`（PTY 側）、ポータブル版実行で終了コード 5 の伝播を確認。UI 表示部は実装済み・実機未確認 |
| 14 | 無効なセッショントークンでは出力を取得できない | **合格** | 統合テスト `認証前はWebSocketに接続できない`、`無効なトークンでは接続できない`、`認証済みクライアントだけが出力を取得できる` |
| 15 | TermWatch が `0.0.0.0` や LAN IP で待ち受けていない | **合格** | 実起動して `netstat -ano` で確認: `TCP 127.0.0.1:43899 ... LISTENING` のみ。コード上も `listen(port, '127.0.0.1')` 固定 |
| 16 | dev tunnels が利用できない場合も LAN や `0.0.0.0` へ自動公開されない | **合格**（設計） | bind アドレスは `127.0.0.1` のハードコードで、変更する設定・分岐が存在しない。フォールバック処理も存在しない。dev tunnels 遮断環境での実挙動は未確認（MT-15） |
| 17 | `--record` 未指定時にターミナル内容がディスクへ保存されない | **合格** | 統合テスト `--record 指定時のみディスクへ保存する`。`--record` が null のとき書き込みストリームを一切生成しない |
| 18 | ZIP 展開後、Node.js を別途インストールせず起動できる | **一部合格** | ZIP を展開し、同梱 `runtime\node.exe` から `--version`・PTY 経由の子プロセス実行・終了コード伝播を確認済み。ただし**確認は Node.js 導入済みの開発機上**であり、Node.js 未導入の別 PC での確認は未実施（MT-14） |

---

## 集計

| 判定 | 件数 | 該当項目 |
|---|---:|---|
| 合格 | 17 | 1〜17 |
| 一部合格 | 1 | 18 |
| 未確認 | 0 | - |
| 不合格 | 0 | - |

補足: 16 は設計上の確認にとどまり、dev tunnels 遮断環境での実挙動は未確認（MT-15）。
18 は Node.js 未導入 PC での確認（MT-14）が残っている。
18 は Node.js 導入済みの開発機での展開・実行のみ確認しており、
Node.js 未導入 PC での確認（MT-14）が残っている。

### 実機確認で見つかり、修正した不具合

| 発見 | 内容 | 対応 |
|---|---|---|
| MT-09 (1) | 複数行の指示が改行位置で分割され、2 つの指示として渡っていた | bracketed paste 内の改行を CR → LF（`7386e78`） |
| MT-09 (2) | 入力はされるが確定されず、追加の Enter が必要だった | 確定の CR を本文とは別の書き込みで送る |
| MT-09 (3) | (2) の修正で単一行が確定しなくなる退行 | 固定時間待ちをやめ、TUI の再描画を検知してから確定する方式へ |
| MT-17 | キーボード表示時に入力欄が画面外へ押し上げられ、下に空白が出る | アプリ本体をビジュアルビューポートへ固定して解消 |
| PC操作 (1) | リモート操作モード中、PC 側に `[?2004h` のような断片が表示される | タイトル書き込みが PTY 出力のエスケープシーケンスへ割り込んでいた。出力が落ち着いてから書くよう変更（D-008） |
| PC操作 (2) | リモート操作モード中、PC 側の矢印キーが効かず `[A` `[B` `[C` `[D` が入力される | 分割到着を疑った 1 回目の修正（D-018）は的外れだった。診断記録で実測し、VS Code ターミナルが win32 input mode で矢印キーを 3 文字分のレコードへ分解して送っていることを特定。復号してから PTY へ渡すよう変更（D-019） |
| MT-02 | 再ペアリング時、ペアリングコードが子プロセスの画面消去で読めなくなっていた | ウィンドウタイトルへも表示（`05b6b0b`） |
| 起動 | `termwatch.ps1` が PowerShell の実行ポリシーで弾かれ、PATH 経由で起動できなかった | `.ps1` を同梱しない（D-016） |

---

## 残る確認作業

利用者側で以下を実施し、この表を更新すること。手順は `docs/MANUAL_TESTS_WINDOWS.md`。

| 手順 | 内容 | 対応する受け入れ基準 |
|---|---|---|
| MT-14 | Node.js 未導入 PC での ZIP 展開・起動 | 18 |
| MT-15 | 障害系（ポート競合・転送停止・強制終了など） | 16 の実機裏付け |
