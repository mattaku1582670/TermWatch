# TermWatch セキュリティモデル

TermWatch は、ターミナルの内容と入力権限という機微な資産を扱います。
このドキュメントは、何を守り、どのように守っているか、そして**何を守らないか**を明示します。

---

## 1. 守る対象

| 資産 | 内容 |
|---|---|
| ターミナル出力 | ソースコード、ファイルパス、APIキー、エラーメッセージなどが含まれうる |
| PTY への入力権限 | 任意コマンドの実行につながる |
| セッショントークン | 上記2つへのアクセス権そのもの |

## 2. 想定する攻撃者

1. 同じ LAN 上の第三者
2. インターネット上の任意の第三者
3. 利用者のブラウザで開かれた**別サイト**（クロスサイト攻撃）
4. 転送 URL を偶然・意図的に知った第三者

想定**しない**攻撃者: PC のローカル管理者権限を持つ者、同じ Windows ユーザーとして
ログインできる者。これらはこのツールの防御範囲外です（PTY を直接操作できるため）。

---

## 3. 多層防御

リモートからターミナルへ到達するには、次の**すべて**を通過する必要があります。

| 層 | 内容 | 防ぐもの |
|---|---|---|
| 1. ネットワーク境界 | `127.0.0.1` のみで待ち受け | LAN・インターネットからの直接到達（攻撃者 1, 2） |
| 2. dev tunnels の認証 | GitHub / Microsoft アカウント認証（Private 可視性） | 転送 URL を知っただけの第三者（攻撃者 4） |
| 3. ペアリングコード | 起動ごとのワンタイムコード | 同一アカウントの他デバイス・URL 漏洩時の悪用 |
| 4. セッショントークン | HttpOnly / SameSite=Strict Cookie | トークンの JavaScript からの窃取、クロスサイト送信 |
| 5. Origin 検証 | Host と Origin の同一オリジン検証 | クロスサイト WebSocket 接続（攻撃者 3） |
| 6. 操作権 | サーバー側での入力可否判定 | 閲覧クライアントからの入力 |
| 7. メッセージ検証 | 許可リスト方式のスキーマ検証・サイズ上限 | 想定外の入力によるクラッシュ・資源枯渇 |

**層 1 は自動的に緩められません。** dev tunnels が使えない場合でも、LAN 公開や
`0.0.0.0` bind へフォールバックしません。

---

## 4. 認証の詳細

### ペアリングコード

| 項目 | 値 |
|---|---|
| 文字集合 | `23456789ABCDEFGHJKMNPQRSTVWXYZ`（30文字） |
| 長さ | 8文字（表示は `XXXX-XXXX`） |
| エントロピー | 約 39.2 bit |
| 有効期限 | 起動から 10 分、または最初の認証成功の早い方 |
| 表示先 | PC 側ターミナルのみ |

`0` `1` `I` `L` `O` `U` を除外し、読み間違い・打ち間違いを避けています。
入力時は小文字・ハイフン・空白を吸収します。

総当たりに対しては、連続 5 回の失敗でロックアウトし、ロックアウト時間を
30 秒 → 60 秒 → … と倍化（上限 15 分）させます。これはプロセス全体で共有する
カウンターです（Private Port Forwarding 経由の到達元 IP は常に `127.0.0.1` になるため、
IP 単位のカウントでは意味をなさないためです）。

### セッショントークン

- 起動ごとに `crypto.randomBytes(32)`（256 bit）で生成します。
- 検証は定数時間比較（`timingSafeEqual`）で行います。
- `HttpOnly; SameSite=Strict; Path=/` の Cookie として渡します。
  HTTPS 経由（`X-Forwarded-Proto: https`）の場合は `Secure` を付与します。
- TermWatch 終了時に無効化します。
- URL、通常ログ、アクセスログ、エラーメッセージへ出力しません。

---

## 5. Origin 検証

WebSocket は同一オリジンポリシーの対象外であるため、明示的な検証が必要です。

- `Origin` ヘッダーが無い接続は**拒否**します（非ブラウザクライアント、偽装対策）。
- `Origin` のホスト部が `Host`（または `X-Forwarded-Host`）と一致する場合だけ許可します。
- ループバックの表記ゆれ（`localhost` / `127.0.0.1` / `[::1]`）は**ポートが一致する場合のみ**許容します。
- 許可リストを広げる設定は提供しません。安全に検証できない場合は接続を拒否します。

`X-Forwarded-Host` を候補に含めるのは、**`Host` がループバックの場合だけ**です
（＝ プロキシ経由で届いたリクエストに限る）。直接アクセスされたリクエストが転送ヘッダーを
自称しても採用しません。加えてサーバーは `127.0.0.1` でしか待ち受けないため、
外部の攻撃者がこのヘッダーを付けたリクエストを到達させること自体ができません。

---

## 6. 入力の検証

- WebSocket メッセージは JSON のみ。バイナリフレームは拒否します。
- 許可リスト方式のスキーマ検証を行い、未知の `type` や型不一致は拒否します。
- 1 回のテキスト入力は **64 KiB** まで。
- WebSocket フレーム全体は 68 KiB まで（超過時は `ws` が接続を切断します）。
- 1 クライアントあたり 10 秒間に 300 メッセージまで。
- 同時接続は 8 クライアントまで（超過は 503）。
- `resume`（スナップショット要求）は 1 接続につき 1 回だけ。繰り返すと接続を閉じる。
- HTTP のヘッダー受信 10 秒、リクエスト全体 30 秒、keep-alive 5 秒でタイムアウト。
- 30 秒ごとの ping/pong で半開き接続を検出し、切断する（操作権の 60 秒解除が確実に働く）。
- 特殊キーは列挙された 10 種類のみ受け付け、サーバー側で対応するバイト列へ変換します。
  クライアントから任意のバイト列を送ることはできません。

## 7. コマンド実行

- `--` 以降は argv 配列のまま `node-pty` へ渡します。コマンド文字列を連結してシェルへ渡しません。
- `.cmd` / `.bat` は Windows の仕様上 `cmd.exe /d /s /c` 経由になりますが、
  この場合も引数は配列のまま渡します（`/d` で AutoRun を無効化）。
- `.ps1` の直接実行は拒否します。
- 子プロセスは TermWatch を起動した Windows ユーザーと同じ権限で動作します。権限昇格は行いません。

## 8. Web UI

- ターミナル文字列は必ず xterm.js へ**データとして**渡します。`innerHTML` などによる
  HTML 挿入は行いません。
- Content-Security-Policy: `default-src 'none'` を基点に、`script-src 'self'` などへ限定します。
  外部 CDN を利用しません（すべて同梱）。
- `X-Content-Type-Options: nosniff`、`X-Frame-Options: DENY`、`Referrer-Policy: no-referrer`、
  `frame-ancestors 'none'` を設定します。
- クリップボードへの自動コピーは行いません。

## 9. データの保持

| データ | 保持場所 | 破棄タイミング |
|---|---|---|
| ターミナル出力 | メモリ（リングバッファ、既定 10,000 表示行） | TermWatch 終了時 |
| セッショントークン | メモリ（＋ブラウザの Cookie） | TermWatch 終了時に無効化 |
| ペアリングコード | メモリ | 認証成功時または 10 分経過時 |
| 送信履歴 | ブラウザのメモリのみ | ページを閉じたとき |
| 生 PTY 出力 | `--record` 指定時のみディスク | 利用者が削除するまで残る |

## 10. 意図的に実装していないこと

- LAN への公開、`0.0.0.0` bind
- Public Port Forwarding の案内・自動実行
- 公開トンネル（ngrok 等）への自動接続
- 独自のクラウド中継
- 永続的な信頼済み端末登録（毎回ペアリングが必要）
- クライアント側だけに依存した入力制限
- 平文トークンのログ保存

## 11. 残存リスク

- **転送 URL の可視性を `Public` へ変更した場合**、URL を知る誰でも到達できます。
  TermWatch 側のペアリングコードが第二の防壁として残りますが、この構成は想定外です。
  Visibility は必ず `Private` のままにしてください。
- 認証済みのブラウザセッションを持つ端末を第三者が物理的に操作できる場合、
  閲覧および（操作モードを有効化すれば）操作が可能になります。端末のロックを行ってください。
- `--record` で保存したファイルは TermWatch の管理外です。取り扱いは利用者の責任です。
- Cookie の有効期限は 24 時間ですが、サーバー側のトークンは TermWatch 終了時に無効化されるため、
  実質的な有効期間はプロセスの生存期間です。
