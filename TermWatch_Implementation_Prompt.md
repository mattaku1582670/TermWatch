# TermWatch実装用 Codex指示プロンプト

以下をそのままCodexへ渡してください。`TermWatch_Requirements_v1.0.md`も同じプロジェクト直下へ置いてから実行します。

---

あなたは、Windowsのターミナル、PTY、WebSocket、TypeScript、Webセキュリティに精通したシニアソフトウェアエンジニアです。

現在のプロジェクトに、Windows PCのVS Code統合ターミナルで動くコーディングエージェントを、スマートフォンからリアルタイムに監視・操作できる「TermWatch」を実装してください。

## 最初に必ず行うこと

1. プロジェクト直下の `TermWatch_Requirements_v1.0.md` を最初から最後まで読み、MVPの唯一の仕様書として扱ってください。
2. 既存ファイル、Git状態、利用可能なNode.js・npm、Windows環境、VS Codeのバージョンを安全な読み取り操作で確認してください。
3. 既存の変更やユーザーのファイルを上書き・削除しないでください。
4. 仕様書と本指示に矛盾がある場合は、仕様書を優先してください。ただし、セキュリティを弱める解釈はしないでください。
5. 実装前に簡潔な作業計画を提示し、その後は計画だけで止まらず実装へ進んでください。
6. 質問がなくても安全に決められる細部は、妥当な判断を行い `docs/DECISIONS.md` に記録してください。
7. セキュリティ、データ消失、既存環境の破壊につながる重大な不明点だけを質問してください。

## 完成させるもの

次のように利用できるTermWatch MVPを完成させてください。

```powershell
termwatch -- codex
termwatch -- codex resume --last
termwatch -- claude
```

TermWatchは指定されたCLIをWindows ConPTY対応のPTY内で起動し、次の入出力を実現します。

- PC側のVS Codeターミナルには、従来どおり完全な入出力を提供する。
- 同じPTY出力をWebSocket経由でスマートフォンへリアルタイム配信する。
- スマートフォンは既定で閲覧専用とする。
- スマートフォンで操作モードを有効にした場合だけ、追加指示や特殊キーをPTYへ送れるようにする。
- スマートフォンが切断されても子プロセスを止めない。
- スマートフォン再接続時に直近の出力を復元する。

## 技術方針

仕様書に従い、原則として次を採用してください。

- TypeScript
- Node.jsの安定したLTS版
- `node-pty`によるWindows ConPTY対応
- WebSocket
- `@xterm/xterm`
- Vanilla TypeScript＋HTML＋CSS
- Windows x64向けポータブルZIP
- VS Code Port Forwarding（Microsoft dev tunnels、Private）

ReactとElectronは使用しないでください。グローバルnpmパッケージのインストールを前提にしないでください。

ポータブル配布では、単一EXE化に固執しないでください。`node-pty`のネイティブモジュールとNode.jsランタイムを同梱したフォルダー形式で構いません。最重要なのは、ZIP展開後にNode.jsやnpmを別途インストールせず起動できることです。

## 必須実装項目

### 1. CLI

次の構文を実装してください。

```text
termwatch [TermWatchオプション] -- <実行コマンド> [引数...]
```

必須オプション:

- `--cwd <path>`
- `--port <number>`
- `--buffer-lines <number>`
- `--control-minutes <number>`
- `--record <path>`
- `--local-only`
- `--help`
- `--version`

`--`以降のコマンドと引数を、安全なargv配列として子プロセスへ渡してください。コマンド文字列を単純結合してシェルへ渡さないでください。コマンドインジェクションを避けるため、通常は`spawn`相当のシェル無効動作を使用してください。

### 2. PTYラッパー

- WindowsではConPTYを使用する。
- ローカル標準入力をPTYへ転送する。
- PTY出力をローカル標準出力とWebSocketへ同時転送する。
- ANSIカラー、カーソル移動、日本語、CodexのTUIを扱えるようにする。
- PC側ターミナルのサイズをPTYの正とする。
- PC側のリサイズだけをPTYへ反映する。
- スマートフォンからのリサイズ要求はPTYへ反映しない。
- 終了時・例外時にraw mode、カーソル、入力状態を必ず復元する。
- 子プロセスの終了コードをTermWatch自身の終了コードとして返す。
- Ctrl+Cは原則として子プロセスへ転送する。

### 3. 出力配信と再接続

- WebSocketでPTYの生出力を配信する。
- 直近10,000表示行を既定値としてメモリ上に保持する。
- 新規接続・再接続時は、スナップショットを送った後にライブ出力へ移行する。
- スナップショットとライブ出力の境界で、欠落や重複が起きにくい連番方式を設ける。
- クライアントが遅い場合にメモリを無制限消費しないよう、バックプレッシャーと上限を実装する。
- WebSocket切断で子プロセスを終了しない。
- ディスクログは`--record`指定時だけ保存する。

行数ベースのリングバッファ実装がANSIシーケンスの途中切断を招く場合は、安全なチャンク保持と表示行上限を組み合わせてください。採用した方式を `docs/DECISIONS.md` に記録してください。

### 4. スマートフォンWeb UI

iPhone縦向きを第一対象として、次を実装してください。

- ダークテーマのxterm.js表示
- 接続状態
- 子プロセス状態
- 最終出力からの経過時間
- 操作モードの残り時間
- 「最新位置へ」ボタン
- 複数行入力欄
- 送信ボタン
- Enter、Esc、Tab、Ctrl+C、上下左右、y、nの特殊キーボタン
- 閲覧モード／操作モード切り替え
- 通信切断後の指数バックオフ付き自動再接続

画面幅320px以上で主要機能を利用可能にしてください。iOSのソフトウェアキーボード表示時にも、入力欄とターミナル現在行を確認できるようにしてください。

複数行の指示は、途中の改行で誤送信されない形で一括送信し、最後にEnterを送ってください。対応可能な場合はbracketed pasteを利用し、対応判断とフォールバックをテストしてください。

Ctrl+Cは危険操作として明確に表示し、実行前に確認を要求してください。

### 5. 閲覧モードと操作権

- スマートフォン接続時は必ず閲覧モードから開始する。
- 閲覧モードでは、UIを改変して直接WebSocketメッセージを送ってもPTY入力できないよう、サーバー側で拒否する。
- 操作モードは明示確認後に有効化する。
- 既定有効時間は10分とする。
- 残り1分で警告する。
- 期限到達時に自動解除する。
- 60秒以上の切断で操作権を解除する。
- 同時に操作権を保持できるリモートクライアントは1台だけとする。
- 複数の閲覧クライアントは許可する。
- PC側入力は常に利用可能とする。
- PC入力とリモート入力は、PTYへ到達した順番を維持する。

PC側へ操作中であることを通知する際、子プロセスのTUIへ文字列を混入させたり、画面を破壊したりしないでください。安全に表示できない場合は、別のローカル状態表示方法を採用し、その判断を記録してください。

### 6. VS CodeのPrivate Port Forwarding限定接続

- Webサーバーは`127.0.0.1:<port>`だけで待ち受ける。
- `0.0.0.0`、LAN IP、パブリックIPへ直接bindしない。
- VS Code組み込みのPort ForwardingがlocalhostへHTTPSプロキシする構成とする。
- VS CodeのPortsビューでポート`43821`を手動転送する手順を案内する。
- Port Visibilityは必ず`Private`とし、`Public`への変更を案内・自動実行しない。
- 転送URLへは、PC側で転送を開始したものと同じGitHubまたはMicrosoftアカウントで認証する。
- TermWatchはdev tunnelの作成、削除、公開範囲変更を自動実行しない。
- VS Code Port Forwardingが利用できない場合、LAN公開へフォールバックしない。
- Windowsファイアウォールの受信許可ルール追加や、ルーターのポート開放を要求しない。

READMEと起動時案内には、次の操作を分かりやすく記載してください。

1. TermWatchを起動する。
2. VS Codeのコマンドパレットから`Ports: Focus on Ports View`を実行する。
3. `Forward a Port`で`43821`を転送する。
4. Port Visibilityが`Private`であることを確認する。
5. Forwarded AddressをiPhoneで開く。
6. 同じGitHubまたはMicrosoftアカウントで認証する。
7. PC側に表示されたワンタイムペアリングコードを入力する。

### 7. セッション認証

- 起動ごとに256bit以上の暗号学的乱数による内部セッショントークンを生成する。
- PC側ターミナルへ、手入力可能なワンタイムペアリングコードを表示する。
- ペアリングコードは、入力しやすさと十分なエントロピーを両立させる。採用した文字数、文字集合、推定エントロピーを`docs/DECISIONS.md`へ記録する。
- ペアリングコードは起動後10分、または最初の認証成功時の早い方で失効させる。
- スマートフォンはVS CodeのPrivate Forwarded Addressを開き、ペアリングコードを入力する。
- 認証成功後、内部セッショントークンを当該クライアントへ関連付け、以後のWebSocket認証に使用する。
- 認証完了までは出力・スナップショット・状態を送信しない。
- 認証完了前の入力を受け付けない。
- ペアリングコードとトークンをURL、通常ログ、アクセスログ、エラー表示へ出さない。
- TermWatch終了時にトークンを無効化する。
- WebSocketのOriginを検証し、HTTPのHostとWebSocketのOriginが同一オリジンでない接続を拒否する。
- localhostまたはVS CodeのPrivate Port Forwardingページと同一オリジンの接続だけを許可する。
- 認証失敗の連続試行にレート制限を設ける。
- WebSocketメッセージをスキーマ検証する。
- 1回のテキスト入力上限を64KiBとする。
- Content Security Policyなど、ローカルWeb UIとして妥当なHTTPセキュリティヘッダーを設定する。

Originを安全に検証できない場合、許可範囲を無制限に広げないでください。安全側へ失敗させ、設定方法を明示してください。

### 8. 状態表示

MVPでは出力内容を解析して「承認待ち」「質問待ち」と意味判定しないでください。次の観測可能な状態だけを使用してください。

- 起動中
- 実行中
- 出力停止中
- 終了
- 再接続中
- PC未接続

「出力停止中」を「入力待ち」と表示しないでください。最終出力からの経過時間を併記してください。

## セキュリティ上の禁止事項

次は実装しないでください。

- `0.0.0.0`での待ち受け
- 一般LANへの自動公開
- VS CodeのPublic Port Forwarding
- ngrok等の公開トンネルへの自動接続
- ルーターのポート開放を前提とする手順
- 認証前のターミナル出力配信
- クライアント側だけに依存した入力制限
- 平文パスワードやトークンのログ保存
- 既定でのPTY出力のファイル保存
- 子プロセスの管理者権限実行
- コマンド文字列の危険なシェル連結
- Web UIへの未エスケープHTML挿入
- dev tunnel設定の自動変更・無断上書き

## プロジェクト構成

保守しやすい構成にしてください。次は例であり、合理的な調整は可能です。

```text
TermWatch/
├─ src/
│  ├─ cli/
│  ├─ pty/
│  ├─ server/
│  ├─ security/
│  └─ shared/
├─ web/
│  ├─ src/
│  └─ public/
├─ tests/
│  ├─ unit/
│  ├─ integration/
│  └─ fixtures/
├─ scripts/
├─ docs/
│  ├─ DECISIONS.md
│  ├─ MANUAL_TESTS_WINDOWS.md
│  └─ ACCEPTANCE_RESULTS.md
├─ dist/
├─ README.md
├─ SECURITY.md
├─ package.json
├─ package-lock.json
└─ tsconfig.json
```

## 品質要件

- TypeScriptのstrict modeを有効にする。
- 不要な`any`を使わない。
- PTY、WebSocket、認証、操作権を明確なモジュールへ分離する。
- 依存関係を必要最小限にする。
- 依存バージョンをロックファイルで固定する。
- ユーザー入力、WebSocketメッセージ、CLI引数を検証する。
- タイマー、WebSocket、PTY、HTTPサーバーを終了時に確実に解放する。
- 秘密情報を例外やデバッグログへ含めない。
- Windowsパス、空白を含むフォルダー、日本語フォルダーを考慮する。
- READMEと利用者向け表示は日本語を基本とする。
- ソース内の識別子は英語で構わない。

## 必須テスト

最低限、次の自動テストを実装してください。

### 単体テスト

- CLI引数解析
- 作業フォルダー検証
- リングバッファ上限
- 出力連番
- トークン生成と比較
- 認証前アクセス拒否
- Origin判定
- メッセージスキーマ検証
- 64KiB入力上限
- 操作権の取得、排他、更新、解除、期限切れ
- 切断60秒後の操作権解除
- 危険キーのエンコード

### 統合テスト

- テスト用PTYプロセスの入出力
- ローカル入力からPTYへの転送
- PTY出力からWebSocketへの転送
- 認証済みクライアントだけが出力を取得できること
- 閲覧クライアントの入力拒否
- 操作クライアントの入力成功
- 再接続時のスナップショット＋ライブ出力
- WebSocket切断後も子プロセスが継続すること
- 複数クライアントの操作権競合
- 子プロセス終了コードの伝達

Windows ConPTY、VS Code Port Forwarding、iPhoneを必要とする項目は、実行環境で可能な範囲まで実機確認してください。確認できない項目は成功扱いにせず、`docs/MANUAL_TESTS_WINDOWS.md`に具体的な手順と期待結果を記載してください。

## READMEに必ず含める内容

- TermWatchの目的
- 動作要件
- ポータブル版の起動方法
- `termwatch -- codex`の使用例
- `codex resume --last`の使用例
- VS Code PortsビューでのPrivate Port Forwarding設定
- GitHubまたはMicrosoftアカウントによる転送URL認証
- ワンタイムペアリングコードによる接続方法
- 閲覧モードと操作モード
- Ctrl+Cの注意点
- ログを保存しない既定動作
- `--record`利用時の注意点
- 公開インターネットへ公開しないこと
- Port VisibilityをPublicへ変更しないこと
- Microsoft dev tunnelsが組織ポリシーで禁止されている場合は利用できないこと
- 終了方法
- トラブルシューティング
- 既知の制約

## ポータブル版

ビルド後、少なくとも次を作成してください。

```text
dist/TermWatch-win-x64/
dist/TermWatch-win-x64.zip
```

ポータブル版には必要なNode.jsランタイム、アプリコード、Webアセット、`node-pty`ネイティブモジュール、ライセンス情報を同梱してください。

ユーザーPCへのグローバルnpmインストール、システムサービス登録、レジストリ変更を要求しないでください。アンインストールは展開フォルダーの削除だけで完了するようにしてください。

ビルド環境と異なるWindows PCでも動かせる構成を目指してください。ネイティブ依存関係のABI不一致が起きないよう、同梱Node.jsと`node-pty`を組み合わせて検証してください。

## 実装の進め方

次の順番を基本とし、各段階でテストしてください。

1. プロジェクト調査と計画
2. TypeScriptプロジェクト構築
3. PTYラッパーとローカル入出力
4. HTTP／WebSocketサーバー
5. xterm.js Web UI
6. 出力バッファと再接続
7. 認証とワンタイムペアリングコード
8. 閲覧／操作モードと特殊キー
9. VS Code Private Port Forwardingの安全な手動設定案内
10. 単体・統合テスト
11. Windows実機検証
12. ポータブル版生成
13. README、SECURITY、判断記録、受け入れ結果の作成

途中でコードの雛形だけを作って終了しないでください。安全に作業を続けられる限り、テスト、修正、ポータブル版生成まで進めてください。

## 検証コマンド

プロジェクトに適したスクリプトを用意し、少なくとも次を実行してください。

```powershell
npm ci
npm run lint
npm run typecheck
npm test
npm run build
npm run package:win
```

失敗したコマンドがあれば原因を調査して修正し、再実行してください。未解決の失敗を隠したり、テストしていない内容を「動作確認済み」と表現したりしないでください。

## 完了条件

次のすべてを満たしたときに完成としてください。

1. `TermWatch_Requirements_v1.0.md`の受け入れ基準18項目を確認している。
2. 各項目の結果を`docs/ACCEPTANCE_RESULTS.md`へ、合格・不合格・未確認のいずれかで記録している。
3. 単体テストと統合テストが成功している。
4. lint、型チェック、ビルドが成功している。
5. ポータブル版フォルダーとZIPが生成されている。
6. READMEだけで初回利用者がセットアップできる。
7. セキュリティ上の禁止事項に違反していない。
8. Windows／iPhone／VS Code Port Forwardingで未確認の内容が明記されている。

## 最終報告

作業完了後、次を簡潔に報告してください。

- 実装した機能
- 主要な設計判断
- 作成・変更した主要ファイル
- 実行した検証コマンドと結果
- 受け入れ基準の合格数、未確認数、不合格数
- ポータブルZIPのパス
- Windows／iPhoneで利用者が行う最終確認手順
- 既知の制約または未解決事項

実装、テスト、パッケージ作成まで開始してください。
